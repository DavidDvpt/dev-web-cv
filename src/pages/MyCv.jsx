import { Container } from "reactstrap";

const { default: CivilState } = require("../components/CivilState");

const MyCv = () => {
    return (
        <Container>
            <CivilState />
        </Container>
    );
};

export default MyCv;
